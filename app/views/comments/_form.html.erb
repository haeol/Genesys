<%= form_for comment, url: post_comments_path(post.id) do |f| %>
<form>
    <div class="form-group">
        <div class="form-group">
            <%= f.text_area :description, class: "form-control", rows: "3", placeholder: "Your comment" %>
        </div>
    </div>
    <div class="form-group actions">
        <%= f.submit "Send", class: "btn btn-one" %>
    </div>
</form>
<% end %>

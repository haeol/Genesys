
<!-- Navigation Bar -->
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="closed-box"><%= link_to image_tag("genesys_logo_blue.png", :class => "logo pull-left"), '/pages/home' %></div>
  <div class="open-box"><%= link_to image_tag("genesys_logo_white.png", :class => "logo pull-left"), '/pages/home' %></div>
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar top-bar"></span>
        <span class="icon-bar middle-bar"></span>
        <span class="icon-bar bottom-bar"></span>
      </button>
      <!-- brand -->
      <a class="navbar-brand"><span id="span-brand pull-left">eneSYS</span></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to 'Home', root_path, {:class => 'btn btn-default navbar-btn'} %>
        <li><%= link_to 'About', '/pages/about', {:class => 'btn btn-default navbar-btn'} %></li>
				<% if !user_signed_in? %>    
          <%= link_to('Login', new_user_session_path, :class => 'btn btn-default navbar-btn') %>
          <% end %>
          <% if user_signed_in? %>
          <%= link_to('Logout', destroy_user_session_path, :method => :delete, :class => 'btn btn-default navbar-btn') %>
        <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
 <!-- End of Navigation Bar -->
